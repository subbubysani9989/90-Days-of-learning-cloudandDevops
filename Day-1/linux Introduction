what is linux : 
Linux is an operating system, like macOS or Windows.

It is also the most popular Open Source operating system, and it gives you a lot of freedom.

how to install windows subsystem for linux 
open power shell as administrator and run WSL -- install 

####
Linux Commands : 
man whoami
ls
clear (ctrl+l)
pwd
ls -la
ls -ltr
cd - Chages the shell working directory
cd .. ( back to the home folder)
cd ~ (homedirectory)
cd / (root)
mkdir 
mkdir -p (create nested folders inside the directory)
touch (create a file)
ls  -l (listing the files)
rmdir (To delete the empty directory)
rm (to remove the file)
rm -v filename (to give the status)
rm -r directoryname (to delete the directory with the content inside it)
rm -rv direcorty name/ (to delete the directory with the content)
rm -ri directoryname/ (it will ask to delete the files inside the directoy)
